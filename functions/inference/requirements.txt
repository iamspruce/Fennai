torch==2.3.0+cu122  # CUDA 12.2 variant
torchaudio==2.3.0+cu122
flash-attn==2.6.3  # For your original script's flash attention
soundfile
librosa
huggingface-hub
noisereduce
requests
python-dotenv  # For .env loading
# VibeVoice deps (from your speak.py)
transformers==4.44.0
accelerate==0.33.0
sentencepiece
protobuf
# Add if needed: flash-attn (already above)